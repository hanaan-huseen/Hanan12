def  areaOfRec(length,width):
    area = length*width
    return area


length = float(input("enter the length"))
width = float(input("enter the width"))
print("area of a rectangle is " ,areaOfRec(length,width))
